# Handoff 2026-09-26 09:40 CST (supervisor out of budget mid-goal)

Goal text: scratchpad goal.txt (copied below as summary). Operator away, full autonomy.
AUTHORITY: tier-0 edits, firmware build+flash, service restarts, live 8081 access, wiring s_ekf into control.
HARD LIMITS: never send idle cmd, MOTOR_BENCH (0x16) or any motor-spin cmd; never flash while armed; arm only on
decoupled firmware and probe-read mymotor.motor1..4 right after, disarm if any >2000; never hard-delete (Recycle Bin);
no key rotation; after every flash: `python -m ground_station.livewatch verify` + disarmed /state check, on failure
reflash last tag (fallback fw-f2b-ekf-20260926 = c011483, backups in firmware_backups/).
Priorities: P0 e2e_validate 8/8 live -> docs/validation/; P1 RTOS obs DONE; P2 bug hunt; P3 pluggable controller;
P4 declutter (/state speed-up NOT needed: measured 7-26 ms, ~220 samples/s).

## Current state (uncommitted in working tree, main @ c011483)
- P3 firmware: API/controller.{h,c} (ctrl_id_e PID0 MRAC1(default) STRUCT2 RBF3 3LAYER4 [2-4 reserved, refused];
  g_ctrl_select, g_ctrl_select_req, g_ctrl_axis_mask; Controller_CheckSwitch switches only disarmed),
  TASK/StabilizerTask.c uses Controller_Update per axis, USER/main.c Controller_Init, uvprojx adds controller.c.
  Host test tests/firmware_host/test_controller.c + stubs/robot_types.h PASS.
  BUILT + FLASHED + VERIFIED live (verify 0 mismatches; g_ctrl_select=1 mask 15 disarmed; /state connected).
- ground_station/service/rtos_api.py cold/stale-prev fix + tests/test_api_rtos.py (4 tests); live-verified.
- .agent-ops/tools/ekf_mode_test.py restores bias_mode 2 (GATED).
- Full pytest tree was running -> .agent-ops/out/pytest-p3.txt (check its tail; rerun if incomplete:
  `PYTHONUTF8=1 python -m pytest -q -p no:cacheprovider`, ~14 min).

## Next steps, in order
1. If tree green: commit the above (no binaries/patch_*.py except OBJ tracked files already tracked), tag
   fw-p3-ctrl-20260926, copy OBJ/JX_FLY.axf/.hex into firmware_backups/, push.
2. P3 service side: add g_ctrl_select to telemetry manifest (ground_station/livewatch/manifests.yaml,
   flight_test_outer slot2 50 Hz or flight_test_position slot3 20 Hz) - check budget with
   ground_station.livewatch.manifest.{ManifestStore,MultiSlotPresetManager,compute_multi_slot_budget}
   (see livewatch/tests/test_flight_adaptive_preset.py:78, must stay budget.safe <=1600 B/s).
   Add latest controller id to session metadata in ground_station/service/core.py:412 _manifest_context (+ test,
   see service/tests/test_recorder.py:128). Runtime-select: probe write of g_ctrl_select_req via a CommandParam like
   bias_mode in firmware_contract.py:495. Regen manifest: `python -m ground_station.platform.capability_manifest`.
   Docs: controller interface, reserved slots, relation to mrac_simplex.variant.
3. vp2 salvage: rec leak in e2e_validate.check_rec (try/finally); ELF mtime reload in wifi_bridge/api.py;
   NIS bug API/ekf.c ~306 `e->nis = y*y/e->R_z` should use s_zz (host test first).
4. e2e live to 8/8 (was 7/8), write docs/validation/ (EKF modes table from .agent-ops/out/ekf-modes.txt,
   e2e results, similar-risk list).
5. Final report (changed / verified how / open / needs operator). Flag for operator: ctrl_enable defaults 0;
   GATED bias default vs AGENTS.md "fixed-at-boot" line; full tier-0 forces allow_agent_arm; props-on/idle tests
   are operator's; STRUCT/RBF/3LAYER are reserved slots only; RC transmitter off -> EMERGENCY failsafe at boot.

## Ops notes
- Build: in USER/: `C:\Keil_v5\UV4\UV4.exe -b -t JX_FLY -j0 JX_FLY.uvprojx -o build_log.txt`;
  flash `python -m ground_station.flashtool.rebuild_and_flash --force --yes`.
- livewatch: `python -m ground_station.livewatch read <sym> --transport swd` (flag after subcommand).
- State route is GET /state (not /api/state). Service restart resets tier-0: re-POST /api/agent/control
  {"mode":"autonomous","tier0_access":"full","allow_agent_arm":false,"source":"operator:supervisor-on-behalf"}.
  Restart = kill python ground_station.service (not agent_mcp), start with --preset flight_test_adaptive, wait 25 s.
  Restart after every flash (stale ELF cache).
- Workers: Windows agy `.agent-ops/win-agy.ps1 -Id <id> -Model <m>` (Claude/GPT pool resets 13:51 CST, weekly 24%
  left); VPS `bash .agent-ops/vps-worker.sh spawn|wait|fetch|...` (Gemini 26% left). Workers never POST to 8081.
  vps/worker/f2b rejected; vp3 superseded, vp4 rejected. Keep reverse tunnel (ssh -R 18081 oc-agent) alive.
- Commit trailer: Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>
