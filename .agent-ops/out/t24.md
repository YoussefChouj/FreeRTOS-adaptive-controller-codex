# T24 Dashboard Validation

1) STATUS: done
2) Files changed:
- `ground_station/service/e2e_validate.py`: Updated script to perform automated dry runs for flight test, check visible panels, stream states and collect console errors as per T24 checks.

3) Verification:
Command run: `python ground_station/service/e2e_validate.py --url http://127.0.0.1:18081/`
Results:
- **Dry runs**: PASS (2/2). Both `t24_pid` (controller=pid) and `t24_mrac` (controller=mrac) ran for 15s and analysis reached `done`. `metadata.json` preset is NOT reachable over HTTP via `/sessions/<id>`, `/health`, or `/state`.
- **Visible-panel map**: PASS. No duplicate panels across tabs.
- **Streaming**: FAIL (0/4). Sampled twice 10s apart. Attitude/gyro changed on slots 0,1,2. However, ALL 4 slots failed the key age check (> 5s). Many keys in `_key_ts` were 280+ seconds old because they were stale (no longer being published in the current preset but still buffered).
- **Console Errors**: PASS (0). No console errors were recorded during the tab navigation.

4) Open questions / risks:
- The streaming test fails on "oldest key age > 5s" because the server holds stale keys indefinitely in the slot dictionary after a preset change, rather than dropping them.
- Active preset is not exposed over any HTTP GET endpoints (checked `/health`, `/state`, `/sessions/<id>`, `/api/agent/state`).
- Battery voltage occasionally renders as "NOT PUBLISHED" depending on the active preset, making polling on a concrete value unreliable; the test polls for the absence of `--`.
