STATUS: partial
Files changed:
- ground_station/service/core.py: Extract `preset` from bridge's layout to `analyse_meta`
- ground_station/service/api.py: Pass `preset` from `analyse_meta` to `run_analysis_and_track`
- ground_station/analysis/flight_test_folder.py: Thread `preset` to `_run_analysis_subprocess` and `_analysis_main`
- ground_station/analysis/flight_report.py: Use `preset` or fallback to `manifest["preset"]` or `session_meta.json`. Fix `session_id` to use `session_dir.name`.
- ground_station/analysis/tests/test_flight_report.py: Add test for metadata fields and skip plot tests if no MPL.

Verification:
- `python -m pytest -q ground_station/analysis`: 139 passed, 3 skipped
- `python -m pytest -q`: 11 failed (all pre-existing or due to headless PTY limits), 1212 passed, 32 skipped

Risks / Open Questions:
- Not verified: Regenerating the report offline for the actual session `20260925-214330-*` because the `/mnt/c/...` Windows path is unavailable on this Linux VPS.
- `signal_map_used` was already correctly computed dynamically in `generate_report`, which runs in the subprocess, so it naturally passes through.
SUBSTITUTIONS: none
