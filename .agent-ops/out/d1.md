STATUS: done

files changed:
  ground_station/service/core.py — added active_preset/preset_loaded_at attrs + set_active_preset() method
  ground_station/service/__main__.py — call service.set_active_preset() after preset load
  ground_station/service/api.py — expose active_preset/preset_loaded_at in GET /health and GET /state
  ground_station/service/tests/test_preset_exposure.py — 11 unit tests (service tracking + HTTP endpoints)

verification:
  pytest -q test_preset_exposure.py          → 11 passed
  pytest -q test_service.py                  → 58 passed
  pytest -q test_service.py::test_route_map_matches_dispatch → 1 passed
  pytest -q test_service.py test_terminal.py::test_health_has_process_rss_mb → 7 passed

open questions:
  - apply_startup_preset is only called at startup; runtime preset switching via HTTP API would need
    a POST endpoint to call set_active_preset() (not in scope for this task).
  - preset_loaded_at is a unix float timestamp; dashboard JS can use it directly with Date().

SUBSTITUTIONS: none
