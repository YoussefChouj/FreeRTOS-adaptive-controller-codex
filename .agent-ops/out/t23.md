STATUS: done

Files changed:
- `docs/research-platform/VALIDATION-2026-09-26-dashboard.md`: Created validation report containing checks and ranked defects.
- `ground_station/service/e2e_validate.py`: Created re-runnable headless E2E validation Playwright script.
- `ground_station/service/browser_smoke.py`: Removed `channel="chrome"` from Playwright launch to fix browser availability issue on VPS.

Verification:
- Run Playwright validation: `~/venv/bin/python ground_station/service/e2e_validate.py --url http://127.0.0.1:18081/` -> PASS (Script generated data for checks)
- Flight test UI dry run: Custom playwright scripts verifying sequential plugin load (>15s) and `t23_pid` / `t23_adaptive` runs -> PASS
- Checked API `/state` and `/api/recording` -> PASS (State updates but UI sidebar remains blank)
- Total checks: 6 run.

Open questions / risks:
- The dashboard UI is fundamentally failing to populate live data into the DOM sidebars despite receiving it from the API, causing a major usability regression.
- `index.html` sequentially blocks on `await fetch()` for 27 plugins, causing a >15 second startup delay before critical UI controls (like the record panel) render.
- The `flight-test-panel.js` extension does not submit a `label` parameter to the `/api/recording/start` endpoint, leaving the recordings unnamed.
- Generated session analysis reports and `metadata.json` files are written to the host's Windows filesystem (`C:\Users\Acer\...`) but are inaccessible via the dashboard API, leaving Key Results metrics unverifiable from the VPS (not verified).
