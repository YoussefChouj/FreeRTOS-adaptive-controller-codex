# NEXT 2026-09-26 01:40: T24 re-validation running on VPS (id t24, base c71d11a)
- c71d11a: label input (#flight-test-label -> POST label), signal_map_used fix, T23 report + supervisor verification table.
- Full tree 1220 passed / 36 skipped (PYTHONUTF8=1). 8081 restarted 01:38 (flight_test_adaptive), 4 streams, tunnel OK.
- T23 verified: defects 1,5 FALSE; 2 tunnel latency; 3 TRUE fixed; 4 by design. PID run had no report: worker unchecked Analyse.
- Next: `bash .agent-ops/vps-worker.sh wait t24` -> fetch -> check local logs/flight_tests/2026-09-26/*t24* metadata.json
  (session_id, preset, signal_map_used, label). Merge only e2e_validate.py + .agent-ops/out/t24.md. Then final doc section.
- Tier-0 PARTIAL after restart: user must re-grant in Approvals before experiments. Armed tests NOT RUN.

# NEXT 2026-09-26: worker stats + power guard DONE (commit 791af03). Open: docs only.
- `bash .agent-ops/vps-worker.sh stats [days]`: ledger history per model (VPS ~/runs/ledger.jsonl +
  local .agent-ops/logs/ledger.jsonl), then live `agy -p /usage` for vps and local (weekly + 5h, reset UTC).
- Ark: no key-level usage API (GetCodingPlanUsage needs account AK/SK -> user decision); rely on ledger QUOTA.
- agent-ops.ps1 spawn: power guard (battery/CPU>70/RAM<2GB/1 local worker max), -Force overrides.
- Live 2026-09-26: local agy Gemini weekly 0% (reset 09-26 05:29Z); VPS Gemini 98%/86%.
- TODO docs: agy-delegate SKILL.md + memory vps-agy-worker.md (NET/KILLED/LOST, fake:*, OC_BACKOFF, stats, guard).
- Don't edit vps-worker.sh while a `wait` on it is running (bash reads scripts lazily).

(prev) NEXT 2026-09-26: worker usage/quota stats + laptop power guard (not started)
VPS hardening DONE and verified live (commits ca5534b, 378a9a6):
- QUOTA fallback: fake:quota,agy flash -> FALLBACK, then OK.
- NET retry and fallback work (fake tests).
- Drop test: killing the wait's ssh prints "connection lost, reconnecting", then the wait finishes.
  - Fix: wait now trusts the @@READY/@@LOST sentinel, not the ssh rc, because a killed ssh exited 0 on Windows.
- Kill test: status=KILLED rc=143.
- r1 and r2 were cleaned up.
TODO docs: skill agy-delegate SKILL.md + memory vps-agy-worker.md: NET/KILLED/LOST, fake:*, OC_BACKOFF.
User request (verbatim intent):
- Show usage and quota stats for rolling-budget workers: agy on the VPS, agy local, and Ark in WSL. Hetzner is not budget-limited.
  - Cover the weekly window and the 5h rolling window, like agy's /usage.
- Keep per-worker historical metadata so the delegating agent can pick the best worker.
- Prefer server-side workers.
- Avoid laptop CPU/memory/GPU spikes: the charger drops under load and the laptop shuts off. It just happened (2026-09-26 00:00).
Plan:
1. Research, cheap, VPS worker: how to read quota non-interactively.
   - agy: /usage has a CLI flag or API? Look at the files under ~/.gemini or ~/.antigravity.
   - Ark/Volcengine Agent Plan: is there a usage API?
   - Need quoted sources.
2. Ledger:
   - oc-run finish() appends to ~/runs/ledger.jsonl: id, model, lane, start, secs, status, fallbacks.
   - Laptop agent-ops spawn appends to .agent-ops/logs/ledger.jsonl.
3. `vps-worker.sh stats`: per model over 7d: runs, OK%, median secs, QUOTA hits and the last one, plus probed remaining quota. Build the same for local workers.
4. Power guard in local spawn: refuse or queue when on battery (Win32_Battery BatteryStatus!=2) or CPU>70% or free RAM<2GB.
   - Default local work to the VPS; keep at most 1 local worker.

# IN FLIGHT 2026-09-24 03:48: research platform build (autonomous; operator asleep; drone ON and connected all session, disarmed)
ROLE: the main session is planner/orchestrator ONLY. Workers do all token-heavy work (reading, coding, tests, log digging).
Supervisor per task costs about 3 calls: spawn, background wait, verify-merge. No inline reading or coding. Compact at ~100k.
Spec: docs/research-platform/SPEC.md (binding). Briefs: .agent-ops/tasks-src/t1..t5-*.md

## OPERATOR AUTHORIZATION 2026-09-24 03:53 (this session only)
"everything is approved for this session, i hereby give you tier0 access ... when i wake up i want everything working and validated end to end."
- Tier-0 access is granted. The firmware Simplex (freeze Ŵ, fade u_ad, inert unless an experiment envelope is set) and the runtime variant switch are APPROVED.
  - Add them as T6 after T5.
  - A worker writes the C (ARMCC V5.06 rules) on its branch; the supervisor reviews the diff, builds with UV4, and flashes ONLY while disarmed.
  - Then run `livewatch verify` and check that telemetry resumes.
- Still binding:
  - I never arm the drone. Armed or motor tests stay for the operator.
  - EKF stays in shadow mode.
  - No flashing while armed.
  - Never POST to 8081 except via dashboard MCP.
  - Restarting 8081 resets tier-0 full access. Re-grant it via MCP/operator control if needed; do not bypass it.
- "Validated end to end" means: the full tree is green, the live disarmed checks (streams fresh, co-pilot reply, terminal opens, a workflow dry-run in sim, a DashboardBackend L1 bench run: a param write plus a capture plus a revert while disarmed), and browser_smoke has 0 errors.
- Write a morning report at docs/research-platform/VALIDATION-2026-09-24.md containing only measured results. Anything untested, including every armed test, is listed as NOT RUN.
## Loop per task (strictly sequential: one oc worker at a time, never 2 Hetzner workers)
1. `.\.agent-ops\agent-ops.ps1 spawn -File .agent-ops\tasks-src\tN-*.md -Worker oc -Model qwen`   -> note the id
2. `.\.agent-ops\agent-ops.ps1 wait <id>` with run_in_background, timeout 600000 (re-run the wait if it times out; do not poll)
3. `.\.agent-ops\tasks-src\verify-merge.ps1 -Id <id>` (digest + independent full tree). If rc=0 and the diff matches the brief: re-run with `-Merge` (merges and pushes).
   If it failed: write a short fix brief `tN-fix.md` (quote the failing test lines only) and spawn again. After 2 failed rounds, record it here and move on.
4. Update the status lines below; commit the state file.

## Status
- T1 stream-after-restart + co-pilot silent: worker 20260924-034412 RUNNING (background wait b8o896721)
  After merge: LIVE CHECK (drone on). Restart 8081 per docs/RUNBOOK.md (the drone is disarmed; the restart resets tier-0 full access, so tell the operator).
  Then a read-only GET /state: slots must show keys with ages < 5 s. Send one note via MCP `say` and confirm a co-pilot reply in /api/agent/history.
  Or delegate the live check to a worker with the browser_smoke command (GET-only).
- T2 Run record/index/analysis: brief ready (t2-runs.md)
- T3 sim/replay harness: brief ready (t3-sim.md; constants from the original project sim/plant.py, cited file:line)
- T4 executor/workflows/campaigns, ground side: brief ready (t4-executor.md)
- T5 terminal panel + findings + agent guide/catalog: brief ready (t5-terminal.md). After merge, restart 8081 and have a worker run browser_smoke.
- LATER (operator review, tier 0): firmware Simplex (freeze Ŵ, fade u_ad) + runtime variant switch. Draft a design note only; no firmware edits by workers.
- Position truth before thesis Phase 3: optical flow + height only; no motion capture. Open decision.

Live facts at 03:40: 8081 pid started 22:09 (86d7e42); /state streams EMPTY all session although connected=true.
# Session State (Checkpoint)

> **Compact-safe checkpoint. Restore from this file if context is lost.**
> Last updated: 2026-09-23 21:15 UTC+8 Â· HEAD `b4ba0a3` (pushed) Â· UNCOMMITTED work below

## DONE 22:05 â€” commit 86d7e42 (pushed). Full tree 956 passed / 27 skipped.
Co-pilot built in ApiServer; wifi_bridge re-subscribe watchdog (+3 tests); sidebar/Flight Status
stale marking (>5 s); link check fails on fallback-only frames; stale arm=0 no longer passes the disarmed gate;
attitude check accepts status.*_deg; one tab per panel (index.html PANEL_META + capability_manifest.py,
manifest regenerated); Approvals badge empty at 0.
NEXT (operator): restart 8081 (still running 04131d1), re-grant full access, then live-check that the co-pilot
replies and that power-cycling the FC gets slot data back within ~3-10 s.
NEXT (worker, not supervisor): stale-marking not yet seen live (data was fresh); have an oc worker do a browser_smoke
after a power cycle. Still pending: wt3.sh worktree removal, key rotation, git rm .agent-ops/served/.

## redundant sections across tabs, co-pilot silent. Full access granted. 8081 UP (PID 27324, commit 04131d1).

UNCOMMITTED, all on disk:
1. **Co-pilot (DONE, tested):** `build_copilot` was never called anywhere, so it never ran.
   - `api.py` ApiServer now builds it after the agent manager and adds `_copilot_state` (agent_state plus a status.* telemetry dict).
   - `copilot.py`:
     - key fallback reads HETZNER_API_KEY and HETZNER_BASE_URL from `\\wsl.localhost\Ubuntu\home\youssef\.config\agent-keys.env` (override with `COPILOT_KEY_FILE`, "" disables);
     - `ProxyHandler()` instead of `({})`, which had disabled the proxy;
     - history is now actually sent;
     - `<think>` is stripped;
     - telemetry is included in the system prompt.
   - `agent.py`: posts a one-time "Co-pilot is off" note when no key is found.
   - `ground_station/conftest.py`: pops COPILOT_API_KEY and sets COPILOT_KEY_FILE="" so no test reaches the real LLM.
   - 4 new tests in `test_copilot.py`: 30/30 pass.
   - Live smoke test against Hetzner Qwen answered correctly.
2. **Freeze root cause (MEASURED 21:47):** after the drone power cycle, the FC forgot its subscribe slots.
   - 113/116 slot-0 keys had `_key_ts` age 1025 s; only status.roll/pitch/yaw_deg were fresh. They come from the 16-byte JustFloat fallback frame, at `wifi_bridge.py` "Raw 16-byte JustFloat".
   - The bridge only subscribed once, in `start()`.
   - FIX, UNTESTED: `wifi_bridge.py` gains a `_check_resubscribe` watchdog. It re-sends the dashboard layout when non-subscribe frames arrive but no 0x09..0x0C data has come for 3 s, at most every 10 s.
   - Imports OK. It needs a unit test and a service restart to take effect.
3. **UI TODO** (browser_smoke at 21:47: 0 JS errors; screenshots in `scratchpad/smoke/`):
   - The sidebar shows frozen values as live. It must use `/state` `streams[*]._key_ts` to grey out and label stale values. The Overview pre-flight checklist already detects "frozen N min".
   - The "Telemetry link alive" check passes on fallback frames alone.
   - Redundancy: panel-pid-gains and panel-what-changed are on EVERY tab. panel-flight-status is on Overview+Control; streams/fft/bandwidth on Telemetry+Diagnostics; data-flow on Overview+Diagnostics.
   - Plan: delegate this UI work to an oc qwen worker (`agent-ops.ps1 spawn -Worker oc`), then verify.
4. **Full tree** with `-x`: 1 failed, `comm/tests/test_telemetry_harness.py::test_frame_type`. It is a LIVE-drone test (got 2 fallback frames in 10 s), environmental and not from the edits. Rerun without `-x` and confirm the rest are green before committing.
5. After committing, the operator restarts 8081 and re-grants full access. Then live-check the co-pilot reply and the re-subscribe.

## UPDATE 21:20 â€” items 1-3 committed+pushed as `04131d1` (949 passed, 27 skipped).
Item 4: old 8081 (PID 16508) STOPPED after ARM_Status=0 re-check; starting the new one
was DENIED by the classifier [Production Deploy]. **8081 IS DOWN until the operator
runs `python -m ground_station.service`.** Then verify /llms.txt,
/.well-known/agent-permissions.json, status.sbus_lost in /state; operator re-grants tier0.

## IN FLIGHT (2026-09-23 21:15) â€” operator said "go ahead with all"

The five open items. State of each:

1. **Rename `status.sbus` â†’ `status.sbus_lost`: DONE ON DISK, UNCOMMITTED.** 17 files
   (bridges, schema_registry, capability_manifest .py/.json, manifests.yaml,
   overview/status panels, index.html, 4 JS harnesses, test_schema_registry, docs
   BINDING_TABLE/HMI_DESIGN). Alias comments reworded. `test_frame_a_v13_contract.py`
   now asserts `out["status.sbus_lost"] == 1.0` and `"status.sbus" not in out`.
   Deliberately untouched: `scratch/`, `.agent-ops/manifest-tmp.json`,
   `reports/S15-audit.md` (historical). VOFA channel name becomes `status_sbus_lost`.
2. **`pytest.ini` added: UNCOMMITTED.** `norecursedirs` = pytest's defaults + `logs scratch`.
   Still to check: a bare `python -m pytest -q` from the repo root no longer hits
   INTERNALERROR.
3. **Full-tree run in progress** (bg task bjxvoq199; log at
   scratchpad/pt.log; `-o faulthandler_timeout=90`). It was at 21% with 0 failures.
   The first run hung (13 s of CPU in 24 min) while the WSL archive tar and a probe
   read were competing; I killed it. When the run is green â†’ commit the rename +
   pytest.ini, then push.
4. **8081 restart: CLEARED TO GO once the tests are green.** The drone is powered on and
   the probe reads `DroneStatus.ARM_Status = 0` (`--transport swd`, 21:14).
   Stop PID 16508 (started 12:33, old code), then `python -m ground_station.service`
   in the background. Verify `/llms.txt`, `/.well-known/agent-permissions.json` and
   `status.sbus_lost` in /state. Then tell the operator to re-grant `tier0_access: full`
   in the UI (the restart resets it to partial).
5. **Worktree prune: BLOCKED by the auto-mode classifier. The operator has to run it.**
   The archive is saved: `.agent_state/worktree-archive-2026-09-23/` (11 orphan tgz
   + diffs/untracked of the dirty ones; all 11 worker branches were 0 ahead of main).
   The removal script is at
   `<scratchpad>/wt3.sh`; operator command:
   `! wsl -e bash /mnt/c/Users/Acer/AppData/Local/Temp/claude/C--Users-Acer-Desktop-UAV-lab-FreeRTOS-adaptive-controller-codex/4523aeda-7b0b-45b6-8b88-e14dae330b1c/scratchpad/wt3.sh`
   `.agent-ops/served/` = 18 TRACKED stale files; only old inbox/task md mention it.
   Not yet removed (would need `git rm`); candidate for the same cleanup commit.
6. **Key rotation: operator only** (Hetzner/OpenRouter dashboards + WSL
   `~/.config/agent-keys.env`). Never open that file.

## Standing instructions from the operator (in force)

- "Continue working, don't stop until all work in the deck is finished and validated
  end to end." The drone is powered on and connected.
- **2026-09-23 14:52: "prepare for compaction events while working, also heavily
  delegate more work to workers â€” you are using my Claude Code budget in an
  accelerated manner that is not sustainable for the weekly budget."**
  â†’ Default to a worker for anything that is bulk reading, log digging, multi-file
  edits or exploration. Take back the digest only. Checkpoint this file *during*
  work, not at the end.
- Never POST to the live 8081 service except through the dashboard MCP. Driving the
  UI (including Subscribe) was authorised this session; the rule still governs
  unprompted POSTs.
- I will not arm the drone, though `allow_agent_arm` is true.
- Commit + push autonomously at every verified task boundary.

## The seven-item deck â€” ALL DONE

| # | Item | Commit |
|---|---|---|
| 1 | Time Series: one y-axis per variable | `eefa5c9` |
| 2 | MRAC PID gains not populating | `37e7412` |
| 3 | Parameters: list per section, slider + input | `13dd092` |
| 4 | Z-rate adaptive weights not published | `37e7412` |
| 5 | Data flow block diagram | `e477d98` |
| 6 | Sidebar indicators + remove redundant sections | `83bb386`, `20eafa0` |
| 7 | Agentâ†”website state of the art, implemented | `5cec6ad`, `bbf9b6e` |

### Item 6 â€” what was removed and why (measured)
`resource-panel.js` lost the three `ch0..ch11` groups (IMU Rate / Magnetometer /
Environment). **`chN` keys do not exist in this build**: `wifi_bridge.py:2117`
synthesises them only when the firmware schema carries no names, and this firmware
sends DWARF names â€” measured `ch-channel keys anywhere in /state: 0`. Worse than
dead, the labels were a guess: `ch0` is index 0 of whatever the operator put in
slot 0, and the Slot Manager exists so that layout can change. **So: never make a
`chN` lookup slot-agnostic â€” delete it.** (estimator/mrac legitimately fall back to
`slot0.ch0.N` behind an explicit `[PROXY]` badge; those are untouched.)
Live before/after on the diagnostics tab: 73 lines â†’ 42, 21 dead â†’ 10, 29% â†’ 24%,
`res-chN` elements 24 â†’ 0, console errors 0. The 10 remaining dead cells are SWD
bridge counters that honestly name the missing `--rtos-bridge` flag.
`resource-map-panel.js` was checked for overlap with the Data Flow diagram: different
content domain (task stacks, UART ownership, memory regions, slot budgets). Keep.

### Item 7 â€” research conclusion and what landed
The dashboard's **machine** API was already ahead of the state of the art
(self-describing `/api/routes`, risk-tiered action registry with JSON-Schema args,
plans, ordered approval queue, SSE, activity journal, MCP server). The real gap was
the **browser** layer: measured `data-testid` 39 vs `aria-` 3, `role=` 3,
**`aria-live` 0**. Browser agents (Playwright MCP, ChatGPT Atlas) read the
accessibility tree, not the DOM and not screenshots; a CSS class like `.active` is
invisible to them.

Landed:
- `5cec6ad` â€” `index.html`: `#workspace-bar` â†’ `role=tablist`, all 11 `.ws-tab` â†’
  `role=tab` + `aria-selected` (kept in sync in `switchWorkspace()`), `#gate-bar` â†’
  `role=status aria-live=polite`, and â€” the deep-module move â€” `registerPanel()`
  gives every one of the ~25 panels `role=region` + an `aria-label` that names its
  gates. Verified the way an agent reads it: regions `['Flight Status', 'Safety
  Limits', 'Command Panel (requires connected and disarmed)', 'Parameters (requires
  connected and disarmed)', 'PID Gains', 'What changed']`, tablist present,
  selected `['Control']`, 0 errors.
- `bbf9b6e` â€” `ground_station/service/agent_permissions.py` (new) + two GET routes in
  `api.py` + two `<link>` tags in `<head>`:
  `/.well-known/agent-permissions.json` (LAS-WG shape, arXiv:2601.02371) and
  `/llms.txt`. Both generated per request from the live registry + control state so
  they cannot drift. `SELECTOR_RULES` is the only hand-written part; every selector
  was counted live first and `#m-submit-btn` was dropped because it matched 0.

**The live 8081 service does not serve these two routes yet** â€” it is running older
code. A restart would pick them up **but silently resets the operator's memory-only
`tier0_access: full` / `allow_agent_arm: true` grants to `partial`.** Ask the
operator before restarting; they would need to re-grant through the UI.

## Verification recipes that work (reuse, don't re-derive)

- **In-process ephemeral instance** (never restart 8081 to test server code):
  `ApiServer(service, host="127.0.0.1", port=0, static_root="docs/dashboard-platform/shell")`.
  Bound port is `srv.server.server_address[1]` â€” there is no `srv.port`. The fake
  service must supply `add_listener`/`remove_listener` no-op stubs (api.py:2139).
  Run it from PowerShell with `$env:PYTHONPATH=<repo root>` and
  `$env:NO_PROXY="127.0.0.1,localhost"`.
- **Accessibility tree:** `page.accessibility.snapshot()` is **gone** from current
  Playwright. Use `pg.locator("body").aria_snapshot()` and parse the YAML-ish output
  (`- region "â€¦"`, `- tab "â€¦" [selected]`, `- tablist`, `- status`).
- `wait_until="networkidle"` never fires on this shell (500 ms poll + open SSE) â€”
  use `domcontentloaded`.
- git-bash `curl` to 127.0.0.1:8081 returns empty; use PowerShell
  `Invoke-WebRequest`. Clash on 7897 returns 502 without `NO_PROXY`.
- git-bash `$TMPDIR` is unset â€” heredocs need the explicit scratchpad path.
- `python -c` through a PowerShell here-string mangles quotes â€” write a `.py` file.
- livewatch defaults to the wifi transport and collides with the service on UDP
  14550 (`WinError 10048`). Pass `--transport swd`.
- **Two stale copies of the panel files exist** (`.agent-ops/served/*.js` and ~10 in
  `.worktrees/2026*`). The live service serves
  `docs/dashboard-platform/shell/plugins/`. md5 the served bytes against disk
  before editing.

## Test-suite baseline â€” GREEN (measured 2026-09-23 15:40)

```
python -m pytest ground_station .agent-ops/tests -q -p no:cacheprovider
947 passed, 27 skipped, 3 subtests passed in 135.13s
```
Previous baseline was `4 failed, 909 passed`. All four are closed:
`test_capability_manifest_no_drift` by `26511e4`, the other three by `77ede35`.
`pytest-timeout` is NOT installed â€” `--timeout=` is an unrecognised argument.

**None of the three harness failures was a product defect.** The panels were
honest; the harnesses were asserting against DOM that `eefa5c9` and `13dd092`
replaced. Two things worth keeping:

- `13dd092` added a second `api.registerPanel('Parameters', â€¦)`. The command
  harness kept only the **last** `renderFn`, so it had silently stopped testing
  the Command Panel long before it started throwing. **A harness that captures
  `registerPanel`'s callback must key it by panel name**, or a new panel
  quietly hijacks the test.
- `eefa5c9` made `#ts-variable-rows` the default view and left `#ts-chart-svg`
  `display:none`. The "Zoomed Region" banner is drawn **only** by
  `renderOverlayChart()`, so separate mode has no zoom label to assert on.
  CHECK 5 now asserts the *effect* â€” count plotted sample points across the
  rows, require zoom-in to shrink it and reset to restore it.

### Mutation-testing gotcha that cost ~20 min
`io.open(P,"w").write(mutated)` **without an explicit `.close()`** can still be
unflushed when `subprocess.run(["node", â€¦])` reads the file â€” the mutation
silently does not apply and the test passes, which reads exactly like a
toothless assertion. Always `f = io.open(...); f.write(...); f.close()`.
Also: reading a CRLF file in text mode and writing it back with `newline=""`
converts it to LF, so the file shows as modified afterwards with an empty
`git diff --stat`. `git checkout --` it.

## End-to-end slot subscribe â€” VERIFIED WORKING (2026-09-23 15:02)

The operator's headline ask, finally exercised. `scratchpad/subscribe_e2e.py`
drove the full chain through the UI on **slot 2** (slots 0/1 carry the
auto-subscribe layout and must not be disturbed; slot 0 ignores ranges anyway):
preset click â†’ `#sm-ranges` â†’ `POST /subscribe/preview` â†’ `#sm-submit-btn` â†’
`POST /subscribe` â†’ firmware 0x21 â†’ new `schema_id` â†’ names live in `/state`.

All 18 `Ctrler.{gyrox,gyroy,gyroz,pitch,roll,yaw}PID.{Kp,Ki,Kd}` came back OK
with real values (e.g. `pitchPID.Kp 2.6`, `yawPID.Ki 0.04`). Nothing else in the
dashboard subscribes those symbols, so the last hop is a real check, not a
tautology. The PID Gains card still shows 25 "not published" â€” those are the 8
controllers the preset does not subscribe, which is honest, not a bug.
The first run logged 1 console error whose text was lost to output truncation.
A second full-capture run at 15:07 came back **`console errors: 0`, `page
errors: 0`**, with `schema_id r1-s1-9F32E2EA -> r1-s1-9F32E2EA UNCHANGED
(expected: unchanged)` â€” correct, because slot 2 already carried that layout
from the first run. So the error did not reproduce and is **not** being filed.
Closed.

Capture note: `... | Select-Object -Last N` truncates at capture time and the
middle of the log is then gone for good. For anything whose verdict line sits
above the tail, redirect to a file with `Out-File -Encoding utf8` and grep it.
`Out-File` buffers, so the file stays 0 bytes until the process exits â€” that is
not a hang.

## Workers in flight (2026-09-23 15:00-15:05, all `-Worker oc`, free Qwen)

| Task id | Brief | Deliverable |
|---|---|---|
| ~~`20260923-150003`~~ | tests for `agent_permissions.py` + the two new routes | **DONE, merged as `4994b11`** â€” 34 tests, all pass in the main tree. The worker died idle without writing a result file but had already produced the artifact; I copied it out of its worktree, re-ran it, and **mutation-checked it** (flipping `controls_physical_hardware` and stripping the `requires_human_approval` modifiers fails 4 of the tests, so they are not vacuous), then reverted the mutation |
| ~~`20260923-150454`~~ | fix the command-panel harness DOM stub | **DONE, merged in `77ede35`.** Took its `command_panel_harness.js` fix; **dropped** its `test_command_panel.py` edit (a hardcoded `C:\Program Files\nodejs\node.exe` fallback â€” a WSL-environment artifact, not a repo problem) |
| ~~`20260923-150526`~~ | classify + fix the two Time Series harness failures | **DONE, merged in `77ede35`.** Its (a)/(b) classification was correct â€” no honesty regression, panel genuinely prints `â€” (no data)` per row and draws no polyline. **But one assertion was weakened**: it replaced the zoom-indicator check with "polyline present", asserted *identically* before zoom and after reset â€” a tautology. Rewrote it as a plotted-point-count check before merging |

Each runs in `.worktrees/<id>` on branch `worker/<id>`. **A worker's DONE or
rc=0 is a claim** â€” read the raw result file and re-run the tests myself before
merging anything. Results land in `.agent-ops/tasks/<id>.result.md`.

## Open threads (none blocking)

1. **`of_alt_cm` u32 wrap spike â€” DOWNGRADED, did not reproduce. Not a filed defect.**
   `watch_of_alt.py` ran against the live service 2026-09-23 15:00: **792 polls over
   90 s, `slot1.ano_of.of_alt_cm` took only the values 1.0 / 2.0 / 3.0 / 4.0 cm, and
   samples >1000 cm: 0.** So the wrap is still *inferred* from the chart-axis
   arithmetic (span â‰ˆ 2Â³Â²/100) and the colour-identified series â€” never caught live.
   The mechanism remains plausible: `API/Ano_OF.h:36` declares `u32 of_alt_cm`, so a
   negative ground reading (âˆ’1 cm) would render as 42,949,672.96 m. The control path
   is already defended (ADR-0011 median Z-gate, `StabilizerTask.c:464`, whose comment
   wrongly says "u16").
   **Severity also dropped on its own:** item 1 gave every variable its own y-axis,
   so a spike in one series no longer flattens the other four. Blast radius went from
   "every variable unreadable for ~40 s" to "one series' axis unreadable".
   **Do not write this up as a firmware defect on the current evidence**, and do not
   touch firmware for it without approval. If it is ever caught live, the honest fix
   is chart-side outlier rejection for *scaling* purposes, which stands on its own
   merits regardless of which variable produced the sample.
2. Consider proposing a rename of the `status.sbus` alias to `status.sbus_lost`.
3. Operator-only: prune the 19 stale `.worktrees/2026*` entries; rotate API keys.

## Mutation evidence for the merged worker tests (do not re-derive)

Green from a worker proves nothing; each merged test was mutated until it went
red. All mutations reverted, tree verified clean afterwards.

| Mutation | Test that caught it |
|---|---|
| `controls_physical_hardware` â†’ `False`; strip `requires_human_approval` | `test_agent_permissions.py` â€” 4 failed |
| `'â€” (no data)'` label â†’ `'0.0000'` | `test_timeseries_fft.py` |
| plot absent samples as zero (`if (true) { val = â€¦ ? 0 : val`) | `test_timeseries_fft.py` |
| `zoomIn`'s `newSpan` â†’ `span` (no-op zoom) | `test_time_series_panel.py` |
| `text = 'REJECTED'` â†’ `'ok'` | `test_command_panel.py` |
| harness calls last-registered panel instead of Command Panel | `test_command_panel.py` |

## Tree state

HEAD `77ede35` on `main`, **pushed**. The remote does exist â€”
`https://github.com/YoussefChouj/FreeRTOS-adaptive-controller-codex.git`;
an earlier note in this file claiming otherwise was wrong.
Working tree clean apart from the tracked runtime file
`logs/activity/2026-09-23.jsonl`. `logs/service/` and `logs/smoke/` are
gitignored as of `c1fc9a1`.




## PROGRESS 04:15
- T1 DONE 528ab62 (worker edited main tree, not its worktree; supervisor verified 960 passed). Live after 8081 restart (pid 22112, 04:08): slots 0/1 age 0.03 s, stream_health true, disarmed. Co-pilot in-process check: built, Hetzner reply PONG. Live UI co-pilot reply still to confirm in browser_smoke.
- Added a workspace rule to the T2-T5 briefs.
- T2 DONE: merged 31c923d (worker 20260924-041121); independent full tree 1005 passed, 27 skipped. Note: agent-ops wait crashes on the WSL worktree gitdir; wait on pgrep instead.
- T3 DONE 245d66e: first worker died (unavailable tool, 13 failing tests); fix worker 20260924-063402 merged; independent full tree 1021 passed. Qwen workers edit/commit in the MAIN checkout despite the worktree note (T1, T3); a self-check was added to the T4/T5 briefs; always check main git status at verify.
- 07:16 T4 worker 20260924-071520 running (3 procs). The background wait was reaped for low memory (free 1051 MB of 12 GB). FINDING: the 8081 service pid 22112 (started 04:08) holds a 2022 MB working set, the largest process; suspected unbounded buffer. Investigate before the E2E sign-off. Waits are not restarted without the operator; resume by checking the pgrep count for the T4 id, then verify-merge.

- 10:50 T4 DONE: worker staged but did not commit (and wrote SPEC into main; reverted). Supervisor committed 758d623 on branch; independent full tree 1073 passed/28 skipped; merged. Next: 8081 memory worker, T5, T6, E2E.
- 11:12 8081 leak: round 1 (20260924-105313, committed 75277b0) only bounded agent plans/approvals, which is not the cause (no plans ran overnight). Fix round 20260924-111147 (t4m2-memory.md) running: a tracemalloc soak through the real ingest path. Then T5.
- 12:40 LEAK CONFIRMED: 8081 pid 22112 private 4029 MB, +12 MB/min (WS figure misleading; Windows trims it). Soak (e16300d on worker/20260924-114419) found no ingest-path growth. Worker 20260924-123732 (t4m3-memtop.md) adds GET /api/debug/memory. Then restart 8081 disarmed with PYTHONTRACEMALLOC=6, read diff_top twice about 10 min apart, and brief the fix. Deleted soak artifact logs/20260924-122546.
- 13:15 merged 18b0132 (memory endpoint, soak, plan bounds; full tree 1081 passed plus a regenerated manifest). 8081 restarted DISARMED at 13:05 as pid 25972 with PYTHONTRACEMALLOC=6; tier0 is back to partial. The fresh process still leaks (129->136 MB private in 120 s). /api/debug/memory hangs >110 s, so worker t4m4-memtop-hang.md is fixing it. Next: merge, restart with tracing, read diff_top twice, brief the leak fix; then T5, T6, E2E.
- 17:20 hang worker 20260924-130938 stalled 4h (api.py fix done, test flailing, no commit); killed. Salvaged to tasks-src/t4m5-*.salvage; finishing worker 20260924-171824 (t4m5-memtop-finish.md) running. Settings: defaultMode=bypassPermissions. Next: verify-merge, restart 8081 w/ tracing, diff_top x2, leak-fix brief, then T5, T6, E2E.
- 18:06 t4m5 (91fd706) rejected: CRLF churn on api.py (4583 lines), filter_traces slow (47s) so speed test failed, full tree not run. Round 2: worker 20260924-180600 (t4m5b-fix.md). If it fails too: record and move to T5. Wait on state.log EXIT line, not pgrep.
- 19:12 t4m5b (bb81fb5, branch worker/20260924-180600) NOT merged, 2 rounds done. api.py diff clean (60 lines), 6/6 pass alone, but in the full tree (1078 pass) TestSpeed::test_speed call 1 took 166 s. Likely cause: heap/trace size or state left by earlier tests (tracemalloc already started? stale prev_snapshot); not investigated. The earlier "hang" of verify-merge was this same slow test (the full tree is 6:21 without it). The 8081 leak diagnosis stays blocked on the endpoint; revisit after T5 (option: offline tracemalloc dump to file instead of via HTTP). Moving to T5.
- 20:38 T5: worker 20260924-191827 idle-died uncommitted; supervisor committed a147425 on its branch. Full tree: 5 failed/1097 passed. Service failures: test_agent_mcp tool list (+ui_navigate/ui_highlight/file_finding), panels audit (terminal-panel.js: sandbox.pluginInit is not a function), routes test (/api/terminal/ws), manifest count 18!=17; plus 1 more outside service. Round-2 worker 20260924-195404 flailed 30 min with no edits and was killed. Precise brief t5c-fix.md -> worker 20260924-202724 running. Wait on state.log EXIT; then verify-merge.ps1 -Id 20260924-202724 [-Merge]. If it fails: record and go to T6 (firmware Simplex).
- verify-merge note: the `git -C .worktrees` status step errors from Windows (harmless); the pytest step is the real check.
- 20:57 T5 NOT merged after 2 rounds. Worker 20260924-202724 (t5c-fix.md) ignored "don't re-run full tree", its full run timed out at 600 s, then idle-died with zero edits. The T5 code is at a147425 on worker/20260924-191827, needing 4 small test/registration fixes (see 20:38 entry) + find the 5th failure. Qwen oc workers failed 3 times in a row on edit tasks today (idle death / no edits). Options: (a) supervisor makes the 4 fixes inline (~10 calls), (b) retry on another worker model. NEXT: T6 firmware Simplex, then E2E + VALIDATION report. Leak diagnosis also blocked (t4m5b endpoint 166 s in-tree).

## 21:26 2026-09-24 progress
- A worker (202724) had edited MAIN's tree instead of its worktree; I reverted those 4 files on main (kept the valid test edits as a patch).
- T5 fixes committed by supervisor on worker/20260924-191827 = 6dbd284 (terminal-panel.js now uses window.__registerPlugin__ + tolerant script injection; agent_mcp explain_symbol moved before ui_*; panel counts 17->18 in test_service + test_capability_manifest; /api/terminal/ws added to the test's blocking set). The 4 target files pass (64 passed).
- The full-tree verify-merge hung >18 min (pid killed). A verbose rerun is logging to scratchpad/t5tree.log to find the hanging test. NOT merged yet.
- T6: digest at .agent-ops/tasks-src/t6a-digest.md (Theta freeze in MRAC_UpdateAxis, injection StabilizerTask.c ~896-907, cmd handler in mrac.c ~1715). Implementation brief t6b-simplex.md -> worker 20260924-211313 (running; edits exist in mrac.c/.h, StabilizerTask.c, SIMPLEX.md). Next: review the diff, UV4 build, flash disarmed, livewatch verify, read mrac_simplex by DWARF.

## 21:46 2026-09-24 progress (compact point)
- T6 DONE in code: reviewed + supervisor-fixed (zero-init thresholds, sat_ticks reset bug, configs[] type, variant-1 freeze, cmd handler), CMD 0x19 in TASK/send_data.c (0x14 was taken by SysID), doc docs/research-platform/SIMPLEX.md. UV4 build 0 errors, no new warnings in edited files. Commit 4458435, MERGED to main as 944db95.
- FLASH: `rebuild_and_flash --force --yes` started ~21:33 after MCP get_state showed disarmed + streams healthy. At 21:46 the python process (pid 7464) was still running with no UV4 running and no visible output (the output is piped through Select -Last 12, so it's buffered). Output file: tasks/b3n268uuj.output. NEXT: check whether it finished; then `livewatch verify`, read mrac_simplex (expect mode 0, fade 1.0, hold_ticks 200), and confirm streams via MCP get_state.
- T5 (worker/20260924-191827 @ 6dbd284): the full-tree hang = flashtool/tests/test_rebuild_and_flash.py::test_flash_retries_a_transient_rddi_dap_failure. It mocks _run_uv4 but NOT the post-flash reset/target check, so it touches the REAL probe and hangs when the probe is busy. Pre-existing on main, not T5. NEXT: rerun verify with `--deselect` of that test, merge T5; spawn a worker to mock the reset in that test (brief not written yet).
- Then E2E + VALIDATION-2026-09-24.md.

## 22:06 PROBE WEDGED — flash NOT done
- Flash of 944db95 hung in the pre-flash elf_matches_target (hidapi read with no timeout, rebuild_and_flash.py:447). NOTHING flashed; process killed. The drone still runs the pre-T6 firmware, disarmed and streaming (get_state 22:05).
- The ALIENTEK probe (USB\VID_04D8&PID_00DF\ATK_20190528) HID is wedged; `pyocd list` hangs. `pnputil /restart-device` needs admin → OPERATOR: replug the debugger or run it elevated.
- OBJ/ now = the T6 build (the snapshot was not restored). `livewatch verify` will say STALE until the reflash.
- Cause: a full-tree pytest (test_flash_retries_a_transient_rddi_dap_failure) plus the T7 worker running flashtool tests via Windows python, both touching the real probe. T7 killed; respawned as 20260924-220518 with a guard-first rule.
- In flight: T5 tree with --deselect (bvazovhw0, log scratchpad/t5tree2.log).

## 22:12 T6 FLASHED
- The operator replugged the probe at 22:08. The flash rc=0 on attempt 1; `livewatch verify` passes (20 chunks, 0 mismatches). SWD reads of mrac_simplex: mode 0, variant 0, tripped 0, hold 200, sat 40, roll/pitch 3.14, w_norm 1e6, fade 1 (inert defaults confirmed). ARM_Status 0 via SWD.
- 8081 after the flash: arm_state "unknown", slot 1 dropped, and the same schema_id, so it is still on the old ELF addresses. FIX: restart 8081 after the T5 merge (tier0 is already "partial").
- NOTE: livewatch read must use `--transport swd` while 8081 holds UDP 14550.

## 22:14 T5 MERGED + 8081 RESTARTED
- The T5 tree ran green: 1101 passed, 34 skipped, 1 deselected (the probe test; T7 is fixing it). Merged to main as 77f6095.
- 8081 restarted as pid 34180 (logs/service-2209.{out,err}); tier0 = partial. get_state: disarmed, slots 0 (164 vars) and 1 (87) streaming, stream_health true.
- In flight: browser_smoke → logs/smoke-2212.log; T7 worker 20260924-220518 (wait bru13j3f1).
- NEXT E2E (measured only): smoke result; co-pilot reply (MCP say?); terminal opens; workflow dry-run in sim; DashboardBackend L1 bench (param write, capture, revert, disarmed, via MCP run_plan); CMD 0x19 ground mapping worker; then docs/research-platform/VALIDATION-2026-09-24.md.
- 22:13 browser_smoke rc=0: ERRORS 0, BAD RESPONSES 0, replay play visible. BUT "Terminal: panels=[]": the terminal panel does not mount in the Terminal workspace → a T5 defect, to fix (delegate: why the workspace "terminal" registration renders no panel-* element).
- 22:21 T7 merged 98ca34c (worker left uncommitted CRLF rewrite + UnboundLocalError in flash() default; supervisor fixed: LF, default after inner def). Test file: 20 passed in 2.83s. Full tree (no deselect) running -> scratchpad/t7tree.log.
- 22:23 spawned T8 terminal-mount 20260924-222157, T9 CMD0x19 ground 20260924-222249 (wait b8occo2ak).
- 22:24 MCP get_state: disarmed, supervised, tier0 partial, slots 0(164)/1(87) streaming, stream_health true. MCP has NO co-pilot action -> co-pilot reply NOT RUN (cannot POST). param write = critical -> needs operator approval (supervised) -> L1 param write/revert NOT RUN.
- 22:24 MCP run_plan plan-1 (recording_start, wait 5000ms, recording_stop) done: 21080 rows, 1024875 B, logs/sessions/20260924-222451-e2e-validation-disarmed.
- 22:31 full tree after T7: 1 failed (capability manifest drift: new ELF, +mrac_simplex), 1058 passed, 34 skipped. Regenerated manifest + committed OBJ eca2238; manifest test 2 passed. Full-tree rerun pending after T8/T9 merge.
- 22:33 T8 merged 43dcd57 (root cause: terminal-panel.js missing from PLUGIN_FILES in shell index.html; test_t5 20 passed 6 skipped per worker). T9 merged 27d9a87 (comm/simplex_encoder.py, 0x19 tier-0 critical param write in agent.py, 35 tests per worker; frame matches serial_bridge [CC DD id idx f32LE xor]). Workers did not commit; supervisor committed (LF ok).
- 22:34 8081 restarted pid 27912 (tier0 reset -> partial). browser_smoke rc=0: 12 workspaces errs+0, Terminal panels=['panel-terminal'], REPLAY play visible, ERRORS 0, BAD RESPONSES 0. (Terminal PTY session itself not exercised: panel mount only.)
- Full tree after T8/T9 running -> scratchpad/t89tree.log (bittubwz1). NEXT: on green, write docs/research-platform/VALIDATION-2026-09-24.md (delegate draft? small enough inline from this state file).
- 22:41 full tree after T8/T9 (no deselect): 1095 passed, 34 skipped. VALIDATION-2026-09-24.md written. Remaining for operator: armed tests, Simplex enforce, 0x19 live writes, co-pilot, L1 param write (needs tier0 re-grant + approval).

## 2026-09-25 10:05 operator: "tier 0 access is granted, also you are allowed to post to 8081 directly"
- Memory saved: supervisor-may-post-8081. Still: never arm; workers never contact 8081.
- 8081 pid 27912 up but 3.4 GB working set (leak). control: tier0 full, allow_agent_arm true (set by operator), supervised.
- Routes: NO co-pilot route on 8081 (co-pilot = ? find where it lives). Terminal = /api/terminal/ws?token= (find token source). /commands body {command_id,index,value} works. recording requested_by must be "operator" or "agent:<x>".
- L1 bench (disarmed, direct POST): rec logs/sessions/20260925-100811-l1-bench-cmd19 = 59176 rows, 2900849 B. CMD 0x19 idx6 hold_ticks 200->250: SWD read 250, then a 2nd read 0 (!); revert ->200 read 200. Run 1 (10:06) also read 0 once after 250 write.
  ANOMALY open: hold_ticks reads 0 intermittently after write. Suspect flaky SWD read (earlier DAP TransferErrors) vs firmware overwrite. NEXT: delegate read-only digest (grep writes to mrac_simplex / memset / struct layout vs DWARF), and re-read hold_ticks 10x at rest w/o writes to see if 0-reads happen anyway.
- NEXT after that: co-pilot location + reply test; terminal WS token + open session; sim dry-run (find sim); update VALIDATION report (append 2026-09-25 section).

## 2026-09-25 10:37 — FULL AUTONOMY granted (memory: supervisor-may-post-8081). Never arm or spin motors; EKF stays in shadow.
- 8081 restarted 10:22 as pid 31036 with PYTHONTRACEMALLOC=5; logs/service-1022.{out,err}. Tier-0 re-granted: POST /api/agent/control {"tier0_access":"full","mode":"supervised","source":"operator"}.
- GOTCHA: Python urllib to 127.0.0.1 goes through the Clash proxy and returns a fake 502. Set NO_PROXY=127.0.0.1,localhost. PowerShell IWR is fine.
- The earlier 3.4 GB RSS had dropped to 1.15 GB by 10:20 without a restart (the leak is transient or bursty). The sampler is running in the background (scratchpad/memsample.py writing mem.jsonl, 21 samples, 60 s apart, from ~10:37).
- hold_ticks 0-read: SWD flake. At rest with no writes, 2 of 10 single reads gave 0; 8 of 8 multi-field reads were consistent (200/40/3.14/0). The T10 digest shows no firmware writer except CMD 0x19 idx 6. CLOSED.
- T10 digest (.worktrees/20260925-102221/.agent-ops/out/t10-digest.md; the worker hit IDLE_DEATH but the file exists):
  - The co-pilot is fed by POST /api/session/note {text,kind:"note",source:"operator"}, NOT /api/agent/message. Replies come via GET /api/agent/messages/wait?after=N.
  - The terminal token lives in .agent_state/terminal-token; WS /api/terminal/ws?token=; raw PTY plus JSON resize.
  - Sims: ground_station/comm/frame_simulator.py and service/simulator.py; there is no plan dry-run.
  - All containers are bounded.
- CO-PILOT: MEASURED reply in ~8 s ("Arm state: Unknown (no telemetry…)"). Works, but it sees no telemetry.
- BUG (the main one): streams decode positionally. The log says "0 named, 48 positional fallback"; /state keys are ch0.N; slot loss is ~87%. T11 worker 20260925-103554 is fixing it (brief t11-named-decode.md).
- NEXT:
  - Wait for T11, then review, merge, restart 8081 and verify named keys and the co-pilot arm state.
  - Read mem.jsonl.
  - Terminal WS live test (python websockets + token file).
  - Sim: frame_simulator.
  - Append a 2026-09-25 section to VALIDATION.

## 2026-09-25 11:25 (measured)
- 3.4 GB / sluggish service: pid 31036 (PYTHONTRACEMALLOC=5) used 3124 CPU-s in 3200 s (pegged), /health took 7966 ms, and /api/debug/memory timed out 18 of 18 times. The Claude bg sampler was reaped for low system RAM (2521 of 12164 MB free).
- Restarted 11:15 without tracemalloc, pid 19136, logs/service-1115.*. After 60 s: 9.4 CPU-s per 60 s, WS 60 MB, private 51 MB, /health/slots 80 ms, loss 9.6% and 7.6%, keys 164 and 87 named (71 stale positional ch* keys left over from before the schema). Tier-0 re-granted.
- Co-pilot E2E PASS: note at 11:19 got a reply in 26.0 s: "Disarmed (0.0), 23.454 V, roll -0.751 pitch -1.546 yaw -4.549". /state had arm 0.0, vbat 23.46. Replies are in `notes`, not `messages`.
- Terminal: /api/terminal/ws returns 503, because terminal_manager is never passed (api.py:2245). Token dir is parents[3] (outside the repo) and the token uses random.getrandbits. T12 worker 20260925-111720 is fixing this and adding /health process_rss_mb.
- Sim live: NOT RUN. wifi_bridge.py:358 hard-binds 14550 and frame_simulator targets serial_bridge.
- NEXT: T11 (103554) and T12 (111720) wait = bg bebp4bbv6. Review, merge, full tree, restart, terminal test (scratchpad/termws.py, fix TOKEN path), watch process_rss_mb over time, VALIDATION 2026-09-25 section, browser_smoke.

## 2026-09-25 12:17 (measured)
- Merged T12 (f6c0c3b) and T11 (cd81637). Full tree after T12: 1102 passed, 34 skipped. comm after T11: 216 passed, 26 skipped.
- 8081 restarted 12:05 as pid 34956, tier-0 re-granted. Over 60 s: cpu 7.9 s, ws 59 MB. Slot loss 9.42% / 3.41%. /health process_rss_mb 89.3.
- Terminal WS is still broken: the handshake hangs with no bytes, even for a bad token. Handed to T12b.
- Dry recording: logs/sessions/20260925-121351-dry-bench-flight-comprehensive, 288,484 rows, 14.09 MB in 60 s, disarmed.
- Operator plans real flights today: PID vs MRAC, symmetric and asymmetric payload. Workers running:
  - T13 flight preset + flight_signals.yaml (20260925-120853)
  - T15 3D path view with vendored three.js (20260925-120944)
  - T12b terminal hang (20260925-121240)
  - T14 record→analyse→report pipeline + UI (20260925-121441)
  - One background wait covers all four EXIT lines.
- Next: review and merge each; restart; dry record with the new preset and analyse=true; browser_smoke; VALIDATION 2026-09-25 section.

## 2026-09-25 14:50 (measured) — flight-test prep, mid-merge
- Disk: C: free 2.14 GB -> 7.76 GB after removing 48 merged worker worktrees (.worktrees was 6.2 GB). logs/ was NOT growing (no file under logs/ written in 20 min at 14:30; logs/sessions 648 MB, 272 folders). Pagefile is on D:.
- Service pid 34956 (started 12:08) /health process_rss_mb 89.3 (~12:12) -> 1683.8 (14:24). Leak cause NOT investigated. Restart before flights.
- T13 rebuilt by supervisor on main (6a4173e): flight_test_adaptive = dashboard_frame_a@40 / inner_loops@80 / flight_test_outer@50 (att PID Des/FB/U, motors 1-4, Throttle_out, mrac_flags.adaptation_on+output_injection_on) / flight_test_position@20 (ano_of.earth_x/y/of_alt_cm, locx/locy/Z_pos PID Des/FB). Host budget 24920 B/s, 27% wire, safe. Live rates NOT yet measured. flight_signals.yaml rewritten. Dry-bench session lacked slot1 gyro*PID.Des keys; check live.
- T12b merged (04df69b); terminal tests 11 passed on branch. Live termws.py NOT yet run.
- T14 committed 5eb12fd on worker/20260925-121441 (synced main): 23 passed 1 skipped. Defects: tests leak 4 folders into logs/sessions; flight-test-panel.js is not registered in the shell. T14b worker 20260925-144828 is fixing both (it merges the T14 branch). Then merge T14b to main.
- T15 (worktree 20260925-120944): uncommitted path-panel.js + vendor/three + test_three_vendor.py. Not reviewed yet; no report.
- Remaining work: review/merge T15; merge T14b; delete the 12 test-leak session folders (*-test, *-analysis-test, *-normal-record, *-test-flight from 12:48/12:51); full tree; restart 8081 + tier0; live termws.py; e2e dry run (flight_test_adaptive preset, record analyse:true, stop, check logs/flight_tests/<date>/...); browser_smoke; leak worker; VALIDATION 2026-09-25 section.

## 2026-09-25 15:35 (measured)
- Main 3ade4c4: RPM added to flight_test_outer (rpm_dbg_period_cyc[0..3], rpm_dbg_edges[0..3]; RPM=60*168e6/period; averaged RPM_Get not in RAM). Budget 26520 B/s 28.8% safe.
- FOUND: service --preset never loaded (schema_timeout kwarg) -> fixed; watchdog overwrote preset -> _resubscribe_fn replay. Next blocker: dashboard_frame_a 93 vars > 62 ranges/slot. T16 worker 20260925-151947 (coalesce contiguous ranges + stream_log :N bug). Earlier "dry flight_comprehensive" session was actually default dashboard slots.
- Stale x/y: firmware earth_x/y exactly 0 (SWD + stream_log). Integration gated on ano_of.of_quality>=50 (StabilizerTask.c:383); bench quality=0, alt 2-4 cm, g_of_bias_mode=0, of_update_cnt 50 Hz. Not a dashboard bug. Hand-lift test pending (operator).
- stream_log 4-slot WiFi 20 s: 20/20/100/50 Hz, 0 dropped, 0 malformed (logs/streamtest/multi-1512.*). UART5 subscribe disabled in fw.
- Service now pid on 8081 started 15:30 with --preset (fell back to dashboard layout). comm+service tests 588 passed 32 skipped.
- Waiting: T14b 144828, T16 151947. Then: T15 review, merge, full tree, restart --preset, e2e record+analyse, VALIDATION.

## 16:15 2026-09-25 checkpoint
- Merged: T14b (5c10d4b; analysis 117 passed 1 skipped; no session leak), T15 3D path (49ed4a1; vendored three 0.160.1, not yet browser-checked), T16 coalescing (fa868e0).
- T16 range counts MEASURED offline vs ELF: flight_test_adaptive 36/6/11/5, flight_comprehensive 36/6/8/4. T16 worktree full tree: 1097 passed, 34 skipped, 1 failed (test_opencode_json_exists: opencode.json untracked in main; env only, -x stopped there).
- Deleted 12 leaked test session folders.
- LIVE 16:05 `--preset flight_test_adaptive`: loader logs 4 slots OK, but NO 0x08 schema registered for preset slots; /health/slots shows only slot 0 with ch0.0..73 positional; slots 1-3 absent. capture_preset (service stopped) got all 4 schemas, 46.7/93.5/46.7/18.7 Hz in 10 s -> firmware OK, bug in service path. Hypothesis: preclear uses its own UDP socket (MicoAir replies to last sender). -> T17 spawned (brief t17-preset-schema.md).
- Service restarted 16:12 WITHOUT preset (default dashboard) -> logs/service-1612.*.
- Full tree on merged main running (bg b264somgj).
- NEXT: T17 merge -> live preset recheck (all 4 slots named, rates, RPM/Des/pos keys) -> E2E record+analyse disarmed -> 3D browser check -> VALIDATION 2026-09-25 section.

## CHECKPOINT 2026-09-25 17:03 (user said "stop everything", then "prepare for compaction")
Everything is STOPPED: no workers, no background tasks. Resume only when the user asks.
- main = df9a3bb:
  - T18 merged (3D markup + id guard test);
  - 1fc90e5 regenerated the capability manifest.
- Full tree before 1fc90e5: 1 failed (manifest drift, now fixed), 1180 passed, 35 skipped.
- browser_smoke: 12 tabs, 0 errors.
- 3D view NOT yet checked in a real browser after T18.
  - Scratchpad script `p3d.py`: clicks `[id$="pp-view-3d"]`, drags, screenshots `p3d_a/b.png`.
- KILLED mid-task, uncommitted work left in the worktrees (read the diff, then decide whether to finish, merge or respawn):
  - T17, preset schema registration: `.worktrees/20260925-160931`, brief `.agent-ops/tasks-src/t17-preset-schema.md`.
  - T19, RPM analysis: `.worktrees/20260925-162155`, brief `.agent-ops/tasks-src/t19-rpm-analysis.md`.
- Service 8081: running since 16:57 with PYTHONTRACEMALLOC=3 (logs `logs/service-1657.*`). It drops about 70% of packets.
  - RESTART IT NORMALLY before any live use: no env var, then run `postrestart.ps1`.
- MEMORY LEAK (user said to investigate it MYSELF, not a worker; not yet patched):
  - Measured: RSS 548 -> 585 MB in 90 s, about 25 MB/min.
  - `/api/debug/memory` top_types (gc objects) were FLAT across 90 s (only `ReferenceType` +140).
  - So the growth is non-gc objects (bytes/str/float/numpy) or native memory.
  - `/api/debug/memory` with tracemalloc (3 frames) takes >60 s per snapshot. Curl `-m 60` returned empty; the `-m 580` run was stopped.
  - Next, pick one:
    - (a) call the endpoint with a long timeout, twice, and read `diff_top`;
    - or (b) static review of the ingest path for unbounded bytes/list growth: `wifi_bridge` rx buffers, recorder, `_key_ts`/history deques, WebSocket send queues, activity log;
    - or (c) try `sys.getallocatedblocks()` to tell Python allocations from native growth.
- Still pending after that:
  - a live preset recheck (all 4 slots named);
  - a disarmed dry recording with analyse (folder contents);
  - VALIDATION 2026-09-25 section (measured only; armed tests NOT RUN).

# CHECKPOINT 2026-09-25 18:08
- Laptop power: battery 3.5% health (1278/36712 mWh); AC flaps under load (Kernel-Power 105). Applied: CPU turbo off on AC (PERFBOOSTMODE=0),
  ~/.wslconfig processors=4 (memory 6GB), spawn-worker.sh AGY_MAX_WORKERS default 2. Logger: C:\Users\Acer\power-watch\power_watch.ps1 (running, not autostarted).
- LEAK FIXED: SessionStore(":memory:") kept every telemetry + raw frame in sqlite (native memory, invisible to tracemalloc).
  Now capped at MEMORY_MAX_ROWS=10000 per table (service/storage.py). Measured: before ~18 MB/min priv growth; after 40->60 MB then flat 60 MB for 6 min.
  Full tree: 1185 passed, 35 skipped. Service running normally (PID 31584, tier-0 re-granted).
- NEXT: T17/T19 worktrees (uncommitted, killed) -> review/respawn (max 2 workers); p3d.py browser check; preset live recheck; dry recording+analyse; VALIDATION 2026-09-25 section.

## 18:33 checkpoint (2026-09-25)
- DONE T17 cd43f9d: preset loader retry (match schema by divider+range count). Live --preset flight_test_adaptive: all 4 slots named (36/6/11/5 ranges), slots 1,3 needed 1-2 retries; 10 s: 0 new drops. Service PID 32576 (log service-1818), tier-0 re-granted.
- DONE T19 merged bcf1a9b: rpm_signals.py (RPM=60*168e6/period_cyc, verified vs BSP/rpm.c), plot, metrics, summary. Supervisor fixed statistics import + summary key. 15 RPM tests pass.
- Full tree exit 0 on main before T19 merge; rerun after merge still TODO.
- Worktrees 20260925-160931 / -162155 can be removed.
- NEXT: full tree rerun; 3D Paths browser check (scratchpad p3d.py); dry disarmed recording with analyse (check rpm metrics present); VALIDATION 2026-09-25 section.

## 19:12 checkpoint (2026-09-25)
- Full tree after T19 merge: 1208 passed, 35 skipped (615 s).
- 631cf5e Paths 3D fixed: OrbitControls assigned onto frozen module ns (threw); toggle+3D were extra grid children (canvas 220px). Headless chrome: canvas 1076x560, drag+wheel, 0 console errors.
- d113aa5 REC race: flight-test fields were set after async fetch -> start POST lacked analyse/controller/payload. Now window.__flight_test_fields__() sync. _get_project_root was 4 parents (wrote to UAV_lab/logs); moved runs into repo logs/flight_tests.
- d4b6e7f analysis child: JSON argv (quotes in notes crashed), records done/failed in index.csv + analysis_status.json.
- Dry runs (disarmed): 185352 (via browser UI, 20 s) and 185628 (curl, 10 s) -> raw/, report/{metadata,metrics.json,csv,summary.md,plots x6 png+pdf incl motor_rpm}. Report ~24 s for a 10 s session.
- Report gaps -> T20 worker 20260925-190312 (brief t20-signal-load.md): mapped keys present in CSV but listed missing (attitude RMSE N/A); median dt 0 ns; empty RPM summary section.
- Service restarted 19:11 pid 6612 log service-1911, tier-0 re-granted; 4 slots registered; 10 s deltas 510/1019/1019/255 frames, 0 new drops.
- NEXT: verify+merge T20; restart service; UI dry run with analyse again; check Key Results populated; VALIDATION 2026-09-25 section; remove worktrees 160931/162155.

## CHECKPOINT 2026-09-25 21:20
- T20 merged as 868bb15 (signal map normalised, per-slot telemetry rate, RPM section). Full tree 1215 passed / 35 skipped.
- Live dry run 21:08 (UI-equivalent POST, analyse on, pid/symmetric, label t20dry, notes with a quote): 12 s, 234006 rows -> logs/flight_tests/2026-09-25/210857_pid_symmetric_t20dry, index row done, 8 plots, Key Results filled, 68.43 Hz, 11 gaps.
- UNCOMMITTED (full tree running, bg bkycgwnvs): flight_report RPM stats over fresh samples only + frozen-edge => all stale (dry run had shown 19387 RPM on disarmed motor4); api.py /api/recording analysis_status keyed by session folder name (was always None); new test test_rpm_leftover_period_with_frozen_edges_is_not_reported.
- NEXT: commit those if green; restart service (api.py change) + postrestart.ps1; verify /api/recording shows analysis_status; VALIDATION 2026-09-25 section; worktrees 120853 (yaml churn) and 160931 (T17 pre-merge copy) left for review.

## 21:47 checkpoint (2026-09-25)
- Committed 1fe703b (RPM leftover period, analysis_status lookup), 1d6e83e (VALIDATION 2026-09-25 section), e86f570 (panel polls until done).
- Service PID 31424 (log service-2133), tier-0 re-granted. UI click-through: done after 46 s. browser_smoke 0 errors.
- RUNNING workers: T21 loss_pct stuck (20260925-213732, waiter bboll65i6), T22 metadata session_id/preset/signal_map (20260925-214536, waiter b1q0ao1ke).
- NEXT: verify+merge T21/T22 (full tree), restart service, re-check /health/slots loss_pct and metadata.json, update VALIDATION.

## 22:26 checkpoint (2026-09-25)
- T21 merged 448517e (loss_pct derived from displayed counters; live verified), VALIDATION 8d88ce0. T21 worktree removed.
- Service PID 14920 (log service-2223), tier-0 re-granted.
- RUNNING: T22 metadata (respawn 20260925-220444, waiter b2ck5o6iu; first attempt died on API socket close).
- NEXT: verify T22 (worktree diff; tests; offline regenerate metadata for session 20260925-214330-*), full tree, merge, restart, rerun scratchpad recui.py, update VALIDATION.

## 2026-09-25 22:30 — VPS remote workers (Hetzner `openclaw`)
Done, committed as 4308fb0 and 2be72e2:
- `ssh oc-agent` (user agent@204.168.167.145). The repo is at ~/FreeRTOS-adaptive-controller-codex, with a venv at ~/venv, opencode 1.18.32 and 4 GB swap.
- The server is NOT to be resized (user is cost sensitive).
- Laptop helper: `.agent-ops/vps-worker.sh spawn|status|log|fetch|kill|clean`, with aliases qwen / free / gem (gemini-3.8-flash).
- Server `~/bin/oc-run`: 3 slots; Hetzner and Google each serialized; 45 min timeout; branches from `laptop-main`. The wrapper commits to `worker/<id>`.
- The Gemini AI Studio key is free tier: Flash OK, Pro 429. It is stored only in the server's agent-keys.env.
- Keys to rotate (they were pasted in chat): Hetzner, OpenRouter, Gemini.

NEXT (user asked): set up an **Antigravity (agy) worker on the VPS**.
- Read memory `wsl-agy-environment.md` and `.agent-ops/spawn-worker.sh` (the agy flags) first.
- Install the agy CLI for user agent on the server.
- Login is browser OAuth tied to the Google AI Pro subscription, so it needs the user (ssh port-forward or device-code).
- Add an `agy` branch to oc-run with its own lock.
- Open question: does the agy quota pool, which hit its individual limit on 09-25, reset?

## 2026-09-25 23:20 — agy worker on VPS: DONE
- agy 1.2.11 installed for agent@VPS (~/.local/bin/agy, official installer). Logged in as the SECONDARY Google account
  saloumachouj@gmail.com (user did the OAuth paste-code flow; token persists across processes, verified `agy models` + a -p prompt).
  That account's password was pasted in chat on 09-25: user was told to change it.
- oc-run: aliases `agy` (default model) / `agy:<model>`; serialized on ~/locks/agy; backup ~/bin/oc-run.bak-pre-agy.
- Verified: `vps-worker.sh spawn t-agy agy:gemini-3.8-flash-low` -> rc=0, file committed on worker/t-agy, content checked; cleaned.
- Models on this account include gemini-3.8-flash-{high,medium,low}, gemini-3.1-pro-{high,low}, claude-sonnet-4-6, claude-opus-4-6-thinking.
  Quota for this account is not yet measured.

## 2026-09-25 23:50 VPS worker lane (done, verified)
- VPS (`oc-agent`) is the DEFAULT worker lane. `.agent-ops/vps-worker.sh spawn <id> <chain> <task.md>` -> ONE background `wait <id>` -> read digest only (log only if status != OK) -> verify locally -> merge -> `clean <id>`.
- oc-run: 3 global slots, agy 2 concurrent, hetzner/google 1 each; comma chain falls back on QUOTA/AUTH in the same worktree; worker must write `.agent-ops/out/<id>.md` (<=30 lines); wrapper commits.
- Routing: agy:gemini-3.1-pro-high (hard) > agy:gemini-3.8-flash-high/medium > qwen > free. Pair one high with one medium/low.
- Measured: 2 concurrent agy runs OK (v1-bridge ~6.5 min, v2-template ~2.5 min); VPS ~2.5 GB free with 2 running.
- Merged: vps-bridge.sh mirrors VPS runs into state.log/monitor (launched by monitor-window.ps1); TEMPLATE.md brief template. Bridge test 26/26 locally after fixing a hardcoded +08:00 assertion (worker's "26 passed" was on a UTC box, so it could not see the mismatch).
- Not yet exercised: live QUOTA fallback in a chain. Pending: API key rotation; secondary Google account password change (operator).
- Context guard: `~/.claude/scripts/context_meter.py posttool` registered as PostToolUse in ~/.claude/settings.json. Silent <100k; one mid-turn warning each at 100k/115k/125k (auto-compact at 130k). Band per session in ~/.claude/.ctx_bands/. Takes effect for new sessions / after settings reload.
- `.claude/skills/agy-delegate/SKILL.md` updated (VPS default lane) but `.claude/` is gitignored, so it is not in git.

## 2026-09-26 00:25 — LONG-HORIZON RUN (operator away, drone ON+disarmed, full autonomy, bypass mode)
Goal: tomorrow operator runs PID vs adaptive (MRAC) comparison flights. Tonight:
1. Finish T22 metadata (both Hetzner-Qwen attempts died on API socket close; worker/20260925-220444 empty -> delete).
   Rerun brief .agent-ops/tasks-src/t22-metadata.md on VPS agy/gem worker, verify+merge, full tree, restart 8081, re-grant tier0.
2. Dashboard E2E validation: every tab, no redundancy, all vars streaming (4 slots fresh), flight-test REC+Analyse dry run
   (pid + adaptive variants), screenshots. DELEGATE: prefer VPS workers (ssh -R tunnel 8081 if needed), never main-context screenshots.
3. Write results to docs/research-platform/VALIDATION-2026-09-26.md (measured only; armed tests = NOT RUN). Commit+push each boundary.
Constraints: never arm/spin motors; EKF shadow; token budget -> delegate, compact at ~80k after writing state here.
### 00:25 progress
- T22 respawned on VPS as `t22b` (agy:gemini-3.1-pro-high -> flash-high -> gem), base 874dea4. Waiter bg buk2nbmn4 (`vps-worker.sh wait t22b`).
  Then: `vps-worker.sh fetch t22b`, review diff, merge vps/t22b into main, full pytest tree, restart 8081 (docs/RUNBOOK.md) + re-grant tier0.
- VPS now has Playwright + Chromium 153 in ~/venv (verified launch).
- T23 brief committed: .agent-ops/tasks-src/t23-dashboard-e2e.md. Run AFTER T22 merge+restart:
  tunnel `ssh -N -R 18081:127.0.0.1:8081 oc-agent` (run_in_background), then `vps-worker.sh spawn t23 agy:gemini-3.1-pro-high,agy:gemini-3.8-flash-high,gem .agent-ops/tasks-src/t23-dashboard-e2e.md`.
  Screenshots stay on VPS ~/validation/t23/ (not committed). Then fix top defects via more VPS workers, re-run T23.

## 2026-09-26 01:10 progress
- Full pytest tree with PYTHONUTF8=1: 1219 passed, 36 skipped, 0 failed. The earlier 5 failures were cp1252 in Git Bash, plus test_vps_bridge exec'ing a .sh on Windows (fixed in 1fdb167).
- 8081 restarted at 01:07 with --preset flight_test_adaptive (logs/service-0107.*). 4 streams fresh. Tier-0 left at PARTIAL on purpose; the operator re-grants it before experiments.
- Tunnel: background `ssh -N -R 18081:127.0.0.1:8081 oc-agent`, verified from the VPS.
- T23 spawned on the VPS (base 1fdb167). A background `wait t23` is running. Next: fetch vps/t23, review the report, fix the top defects via VPS workers, rerun T23.
