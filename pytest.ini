[pytest]
# pytest's defaults, plus logs/ and scratch/: they hold ad-hoc capture scripts
# (one calls sys.exit at import) that are not tests.
norecursedirs = *.egg .* _darcs build CVS dist node_modules venv {arch} logs scratch
