# Agent Map Benchmark

This file establishes a baseline for answering common code navigation questions using only `grep` and Serena.

## Questions

1. **What writes `u_gyroy` and how can I watch it live?**
   - Baseline (grep + Serena): TBD turns / TBD tokens
   - Agent Map (`explain`): TBD turns / TBD tokens

2. **Which task runs MRAC and at what rate?**
   - Baseline (grep + Serena): TBD turns / TBD tokens
   - Agent Map (`explain`): TBD turns / TBD tokens

3. **Where is the main entry point for the flight state machine?**
   - Baseline (grep + Serena): TBD turns / TBD tokens
   - Agent Map (`explain`): TBD turns / TBD tokens

4. **How do I subscribe to the EKF roll estimate?**
   - Baseline (grep + Serena): TBD turns / TBD tokens
   - Agent Map (`explain`): TBD turns / TBD tokens

5. **What variables are available in the 'flight_comprehensive' capture preset?**
   - Baseline (grep + Serena): TBD turns / TBD tokens
   - Agent Map (`explain`): TBD turns / TBD tokens

6. **Which function initializes the BMI088 IMU driver?**
   - Baseline (grep + Serena): TBD turns / TBD tokens
   - Agent Map (`explain`): TBD turns / TBD tokens

7. **What is the current safety tier for `API/mrac.c` and can it be modified safely?**
   - Baseline (grep + Serena): TBD turns / TBD tokens
   - Agent Map (`explain`): TBD turns / TBD tokens

8. **How does the PWM driver map to the inner PID axes?**
   - Baseline (grep + Serena): TBD turns / TBD tokens
   - Agent Map (`explain`): TBD turns / TBD tokens

9. **What is the expected base rate (Hz) for the main StabilizerTask?**
   - Baseline (grep + Serena): TBD turns / TBD tokens
   - Agent Map (`explain`): TBD turns / TBD tokens

10. **Where are the firmware globals for the pyOCD livewatch to read?**
    - Baseline (grep + Serena): TBD turns / TBD tokens
    - Agent Map (`explain`): TBD turns / TBD tokens
