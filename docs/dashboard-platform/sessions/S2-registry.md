# S2 — Identity, capabilities, and static registry

Implement firmware build identity, capability discovery, and static descriptor
tables. Generate host JSON and validation tests. Do not change actuator behavior.
Gate: build, flash, discover, compare generated registry, and verify rollback.

