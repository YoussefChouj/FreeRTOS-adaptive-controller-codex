# S9 — Dashboard shell and plugin API

Build the browser shell, connection lifecycle, capability-driven plugin loading,
layout persistence, audit log, live state store, alarms, and common controls.
Verify a plugin can be added/removed without core changes.

