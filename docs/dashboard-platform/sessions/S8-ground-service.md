# S8 — Ground-station core service

Build the local Python service around the existing Wi-Fi bridge and decoder.
Implement schema-aware state, command gateway, session/event/raw-frame storage,
replay, simulator integration, and WebSocket/API surfaces. No rich UI yet.

