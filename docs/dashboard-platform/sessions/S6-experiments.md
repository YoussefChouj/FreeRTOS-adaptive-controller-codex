# S6 — Firmware experiment runtime

Implement the experiment state machine, parameter snapshots, active/shadow
sweeps, settling/measuring windows, abort conditions, restore policy, and event
markers. Validate with simulated sweeps before any airborne use. Gate on safe
abort and exact restoration.

