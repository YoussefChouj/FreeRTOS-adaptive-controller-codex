# S7 — Resource map and RTOS observability

Generate the firmware/resource map from registry, ELF/DWARF, tasks, queues, and
memory sections. Add configurable runtime statistics, stack/heap/queue metrics,
deadline and DMA observations. Gate by timing overhead budget and build-profile
comparison.

