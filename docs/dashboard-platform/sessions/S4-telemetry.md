# S4 — Typed telemetry and transport hardening

Generate typed schemas from the registry, retain multi-slot subscriptions, add
sequence/tick/schema metadata, bandwidth negotiation, loss counters, replay
fixtures, and capability/schema discovery. Preserve legacy compatibility during
the migration. Gate with byte-level and hardware rate/loss tests.

