# S5 — Safety authority and plugin runtime

Centralize output authority, safety preconditions, heartbeat expiry, and conflict
resolution. Introduce static controller/estimator/adaptive-layer descriptors and
disabled/shadow/candidate/active/faulted lifecycle states. Port one baseline PID
and one adaptive path without changing nominal behavior.

